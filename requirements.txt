# Django 웹앱 필수
Django==5.2.6

# 프로덕션 WSGI 서버 (Gunicorn 서비스 유닛에서 사용)
gunicorn>=21.2,<22

# WebSocket 지원 (실시간 게임용)
channels==4.3.1
channels-redis==4.3.0
daphne==4.2.1

# 보안 강화
django-csp==4.0  # Content Security Policy
django-allauth==65.13.1  # 이메일 인증 및 소셜 로그인

# Anthropic Claude API (AI 답변 생성, 자동 칼럼 작성)
anthropic>=0.40.0,<2.0.0

# 환경변수 관리 (.env 로드)
python-dotenv>=1.0.0,<2.0.0

# 이미지 처리 (모델 ImageField 이용)
Pillow>=10.0.0,<11.0.0

# 마크다운 렌더링 (템플릿 필터)
Markdown>=3.5,<4.0

# HTTP 요청 라이브러리 (카카오 로그인 API 호출)
requests>=2.31.0,<3.0.0

# Google Search Console API (방문자 리포트의 노출/클릭/CTR 지표) - 선택
#  미설치 시 방문자 리포트는 GSC 섹션만 건너뛰고 정상 동작한다.
google-api-python-client>=2.100.0,<3.0.0
google-auth>=2.23.0,<3.0.0
#  아래는 OAuth 2.0 클라이언트 ID로 토큰을 1회 발급할 때만 필요 (gsc_authorize, 로컬 전용)
google-auth-oauthlib>=1.1.0,<2.0.0

# 데이터베이스: 현재 SQLite 사용. PostgreSQL 사용 시 아래 주석 해제
# psycopg2-binary>=2.9,<3
