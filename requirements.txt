# Django 웹앱 필수
Django>=5.1,<5.3

# 프로덕션 WSGI 서버 (Lightsail에서 gunicorn 사용 권장)
gunicorn>=21.2,<22

# 정적 파일 서빙(간편 배포용) — Nginx 없이도 서빙 가능
whitenoise>=6.6,<7

# OpenAI API
openai>=1.3.0

# 환경변수 관리
python-dotenv>=1.0.0

# 이미지 처리 (답변/댓글 이미지 첨부 기능용)
Pillow>=10.0.0

# 스케줄링 (AI 답변 자동 생성용)
celery>=5.3.0
redis>=5.0.0

# 데이터베이스: 현재 SQLite 사용. PostgreSQL 사용 시 아래 주석 해제
# psycopg2-binary>=2.9,<3
